# Tiny CPU Path Tracing Renderer

A simple, CPU path tracing renderer implemented using C++ 20. 

## Build

```bash

cmake --preset "default" # default is RelWithDebInfo. see CMakePresets.json for more.

cmake --build build
```

## Run
```bash
./build/TinyCPUPathTracing.exe
```